/*O algoritmo deve pedir o valor em dólares que o turista possui, o valor do dólar
do dia e calcular a conversão pela fórmula: R=valor_dolar * dolar_dia. Exiba o
valor em reais.
*/

//Bibliotecas de funções
#include<stdio.h> //Padrão
#include<math.h> //Matemática
#include<conio.h> //Funções

//Comando Principal
int main()
{//inicio
	
	//Declaração das variaveis
	float R, Valor_dolar, Dolar_dia;
	
	//Variaveis de entrada
	printf("Digite o valor do dolar a ser convertido:");
	scanf("%f", &A);
	printf("Digite o valor do dolar neste dia vigente:");
	scanf("%f", &B);

		//Processamento
		
		R=valor_dolar * dolar_dia
		
		//Variaveis de saída
		printf("O Valor em reais e de: %f", R);
		
		
		
		//getch()-tem a função de Imprimir o resultado no display (biblioteca conio.h)
		getch();
}